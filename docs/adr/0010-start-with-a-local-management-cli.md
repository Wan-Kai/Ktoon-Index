# 第一阶段使用本地管理 CLI

第一阶段不部署远端 Content API 或 GitHub App；管理面由本地 CLI 实现，CLI 复用维护者的 `gh auth` 身份并通过 GitHub API 直接修改内容文件、提交到 `main`，不接触本地工作区。文件 SHA 或内容版本冲突时直接失败，不自动合并。这个低成本方案无法阻止拥有仓库写权限的人绕过 CLI，项目明确接受该 MVP 取舍；远端强制管理面和委托身份延后到出现真实需求时再实现。
