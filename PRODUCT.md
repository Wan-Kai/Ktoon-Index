# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

尚未决定。当前阶段先完成产品需求与界面设计讨论，不提前锁定框架、部署方式或内容存储方案。

## Users

主要用户是站点所有者本人。典型使用场景是浏览自己长期积累的 AI 相关内容，重新激活记忆，并快速找回过去形成的判断。

站点内容完全公开，其他开发者、同行、面试官或合作方可以阅读，但公开展示是次要结果，不以陌生访客增长、转化或运营为产品目标。

## Product Purpose

产品是一个个人 AI 知识站，用于长期沉淀值得复用和回看的 AI 信息，包括工具与 Skill、Agent 工程实践、行业标准、产品案例、阅读与实践笔记，以及个人创意。

产品的首要成功标准是：所有者打开站点后，可以通过清晰分类自然浏览内容、重新想起已有知识，并通过简单搜索找回具体条目。它不是 AI 新闻站、通用工具导航站、内容社区或后台管理系统。

## Positioning

这是一个由个人判断驱动、由 Agent 维护、以公开只读方式呈现的 AI Library。内容价值不由热度、点赞或用户投票决定，而由所有者统一使用「夯 / 人上人 / NPC」进行评价。

与普通书签工具相比，它保留条目的个人结论和正文沉淀；与常见 AI 目录相比，它不追求收录规模、SEO 流量或商业推荐；与传统数字花园相比，它以固定分类浏览和简洁列表为主要入口，而不是依赖知识图谱或链接漫游。

## Operating Context

- 公开网站只承担浏览与阅读，不支持站内新增、编辑、删除或管理登录。
- 所有者通过自然语言向 Agent 表达维护意图。
- Agent 经过鉴权后，通过受约束的维护能力查询、新增、修改、评分、归类、打标签、软删除或恢复单条内容。
- 维护能力的具体封装形式可以是 CLI、MCP 或 Skill，当前只确认 Agent 优先，不要求人工直接操作 CLI。
- 每次维护操作返回条目链接和变更摘要，作为可核验回执，而不是人工审批节点。
- 安全依靠窄工具能力、结构化参数、可恢复删除和版本历史实现，不依赖 Agent 主观谨慎或操作前审核。

## Capabilities and Constraints

### 固定分类

第一期一级分类固定为以下五类，顺序不可改变，且 `Ideas` 始终位于最后：

1. `Toolkit`
2. `Products`
3. `Articles`
4. `Standards`
5. `Ideas`

五个分类同等重要。单条内容可以归属多个分类，只保存一份实体，不因多分类产生副本。

### 内容结构

所有内容共用统一结构，不为不同分类建立专属模板：

- 标题：必填。
- 一句话描述：必填。
- 所属分类：必填，至少一个，可多选。
- 评分：选填，限定为 `夯`、`人上人`、`NPC` 或未评分。
- 标签：选填，可多选；标签为扁平自由标签，不存在层级、分组或父子关系。
- 链接：选填；不设置独立的来源、作者或平台字段。
- 正文：选填，自由编写。
- 录入时间：自动产生。

### 浏览与排序

- 首页以五个分类为主入口。
- 每个分类展示内容总数、该分类评分最高的三条内容，以及查看全部入口。
- 条目轻预览统一只展示标题、评分和一句话描述。
- 分类页默认按评分排序：`夯 > 人上人 > NPC > 未评分`；同一评分内按录入时间倒序。
- 分类页支持切换为按录入时间排序。
- 未评分内容不参与首页 Top 3，并在评分排序中位于最后。

### 筛选

- 分类页支持标签筛选和时间筛选。
- 标签筛选一次只能选择一个标签，可与时间范围和排序方式组合。
- 时间筛选只依据录入时间，固定范围为：全部、最近 7 天、最近 30 天、最近一年。
- 不支持子分类、内容类型筛选、多标签组合、自定义日期范围或更新时间筛选。

### 搜索

- 全站提供常驻搜索入口。
- 搜索只进行字符串匹配，不使用语义搜索。
- 搜索范围仅包含标题和一句话描述，正文不参与。
- 相关度顺序为：标题完全匹配、标题包含、描述包含。
- 搜索结果只展示标题、评分和一句话描述，不提供二次筛选。

### 详情阅读

- 详情展示标题、评分、一句话描述、所属分类、标签、链接、录入时间和正文。
- 正文结束即结束，不展示相关推荐、热门内容或算法推荐。
- 保留返回所属分类的入口。

### Agent 维护边界

- 首版仅支持单条操作，不提供批量维护。
- 所有字段通过结构化校验，分类和评分使用限定值。
- 有链接的内容按链接硬去重；重复新增时拒绝创建并返回已有条目。
- 无链接内容不做模糊去重，避免误判。
- Agent 删除条目时只能移入回收站；物理清除能力不向 Agent 开放。
- 所有修改自动保留历史版本，可追溯、可恢复；版本历史不进入公开阅读界面。
- Agent 可以自主执行已授权操作，不需要逐次人工确认。

### 公开范围与国际化

- 全部内容公开可读，不支持私有条目或分级可见性。
- 不提供登录注册、投稿、点赞、收藏、评论、用户评分、分享计数、访问量或订阅功能。
- `夯 / 人上人 / NPC` 原样公开展示，并在首次出现处用一句短说明解释顺序。
- 支持中英文界面切换，切换范围仅限导航、分类名称、按钮、筛选项和提示语。
- 条目标题、描述和正文保持录入时的原语言，不要求维护双语副本。

### 明确未决定

- 产品名称与品牌资产尚未确定。
- 技术栈、部署方式、内容存储和 Agent 接口形态尚未确定。
- 首页具体布局与视觉方向尚未最终确定；当前仅有单列布局偏好，仍需在后续设计阶段验证。
- 产品专属无障碍验收标准尚未确定。

## Brand Commitments

- 对外保留「夯 / 人上人 / NPC」这一具有个人风格的评分语言。
- 产品声音应表达个人判断，而不是伪装成客观行业榜单。
- 不使用“最全”“最佳”“收录数量领先”等规模或权威性宣传。
- 不为公开访客增加营销首页、个人履历包装或增长导向模块。

## Evidence on Hand

- 已完成围绕用户、分类、内容结构、评分、搜索、筛选、详情页和 Agent 维护边界的逐项需求访谈。
- 已调研 Maggie Appleton、Raindrop 公开收藏、Sublime、Pinboard、Cosmos、Andy Matuschak Notes、Are.na 及若干 AI 工具目录的内容组织方式。
- 当前项目目录为空，没有可继承的生产代码、设计系统、品牌素材、真实站点数据集或用户行为数据；未来工作不得虚构这些资产或证据。

## Product Principles

1. **个人优先。** 首先优化所有者的浏览、回忆和找回体验，公开访问不改变这一优先级。
2. **分类先于算法。** 用稳定的五分类和明确排序组织内容，不依赖推荐流、热度榜或知识图谱。
3. **判断高于收录。** 内容规模不是目标，个人评价、描述和长期复用价值才是核心资产。
4. **低阻力沉淀。** 最低录入只要求标题、一句话描述和至少一个分类，允许后续补充评分、链接与正文。
5. **机制保证安全。** Agent 自主维护建立在窄接口、硬校验、软删除、版本历史和可核验回执之上，而不是审批流程。
